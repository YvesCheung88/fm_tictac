import { describe, expect, it } from "vitest";

describe("test", () => {
  it("sums", () => {
    /* 
            expect( actual: valeur obtenu ) 
            .toBe(...) : valeur attendu
        */
    expect(1 + 2).toBe(3);
  });
});
